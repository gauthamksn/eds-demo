indices:
  demoIndex:
    include:
      - demo/**
    target: /demo-index.json
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')
      noteContent:
        select: body div.note
        value: |
          textContent(el)